services:
  editor_service_api:
    build: ./api
    environment:
      - PORT=${EDITOR_SERVICE_PORT}
    ports: 
      - "${EDITOR_SERVICE_PORT}:9004"
    networks:
      - "peerprep_network"

networks:
  peerprep_network:
    name: peerprep